#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd -- "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

usage() {
  cat <<'EOF'
Usage: ./unlink <name>

Names:
  all
  zshrc
  zshrc-public
  config-git-aliases
  config-starship
  gitconfig
  local-bin-git-alias
EOF
}

name="${1:-}"

case "$name" in
  all) exec "$ROOT_DIR/scripts/unlink-all.sh" ;;
  zshrc) exec "$ROOT_DIR/scripts/unlink-zshrc.sh" ;;
  zshrc-public) exec "$ROOT_DIR/scripts/unlink-zshrc-public.sh" ;;
  config-git-aliases) exec "$ROOT_DIR/scripts/unlink-config-git-aliases.sh" ;;
  config-starship) exec "$ROOT_DIR/scripts/unlink-config-starship.sh" ;;
  gitconfig) exec "$ROOT_DIR/scripts/unlink-gitconfig.sh" ;;
  local-bin-git-alias) exec "$ROOT_DIR/scripts/unlink-local-bin-git-alias.sh" ;;
  help|-h|--help|"")
    usage
    ;;
  *)
    printf 'Unknown unlink target: %s\n\n' "$name" >&2
    usage >&2
    exit 1
    ;;
esac
